#include <bits/stdc++.h>
using namespace std;

#define npk ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

typedef long long ll;

double x;

int main() {
    npk;

    cin >> x;

    cout << fixed << setprecision(1) << ((0.5)*x)*(x<=3);
}
